# REMOVED (2026-08-15 security hygiene): this file was a `vercel env pull`
# artifact committed by mistake. All secret values were empty and the only
# credential was a short-lived OIDC token that expired 2026-04-24 — nothing
# live was exposed. Real values belong in Vercel project env settings, never
# in git: GitHub Secret Scanning revokes any token committed to this repo.
# Pull a fresh local copy with `vercel env pull` (output is gitignored).
