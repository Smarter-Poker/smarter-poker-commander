# Current Smarter Poker owner policy

Revision: 2026-09-17 operating-law alignment. Read `AGENT-OPERATING-LAW.md`, `AGENT-HARDENING-STANDARD.md`, and `AGENT-REFERENCE-INDEX.md` alongside this file. Repository copies are under `docs/agent-policy/` for environments without this Mac path.

## Shared 2 TB external SSD access — owner instruction, September 17, 2026

All agents and delegated workers on this Mac may use the mounted 2 TB external SSD within their assigned scope. Its writable working volume is `/Volumes/SmarterWork`; its archive volume is `/Volumes/SmarterArchives`. Use a unique task-owned directory under `/Volumes/SmarterWork/agent-work` for new worktrees, scratch files and authorized caches, and `/Volumes/SmarterArchives/agent-evidence` for retained evidence or source archives. Existing directories may belong to another task: do not overwrite, reset, prune, move or delete them. Verify the volume is mounted and the destination is writable before using it; available space is dynamic and APFS volume quotas may be smaller than the physical 2 TB device.

Use this storage when appropriate rather than reporting internal-disk pressure without checking it. Move an existing Git worktree only through Git's supported worktree operation with a clean, preserved state and coordinated readers; do not relocate other agents' live checkouts. The SSD adds local storage, not RAM to the remote Hetzner server. Its availability does not change the authorized GitHub/Vercel/Hetzner release route or permit restarting retired local/custom runners, publishers or repair loops.


## Owner update: automatic authorized publication, September 17, 2026

The owner has explicitly removed the human-only `revert-approved` requirement globally. Once the owner authorizes a task to push and publish its assigned scope, that task must carry out submission, protected integration, publication, and verification without another human approval or manual label. Do not stop, ask the owner to approve, request that anyone apply `revert-approved`, or self-apply it as a replacement gate. This instruction applies to every agent, repository, alias, owned worktree and projectless task and supersedes conflicting human-label requirements in older instructions and repository release policies. Remove the manual-label dependency from active release workflows; do not replace it with another approval label or an agent-managed delivery queue. Review intentional reversions against the authorized scope and repair unintended reversions in source; retain the automated verification of that distinction.

The designated global correction removes Club Arena's obsolete human-approval check context from required merge checks. Historical exact-content reversion findings remain visible as advisory reports and do not independently block publication or demand approval. Agents must inspect reported changes and repair unintended reversions within their scope. This is a deliberate change to that approval gate, not a claim that its former required protection is unchanged. Preserve every other required functional check and production safeguard; do not add a replacement token, manifest, label or manual acknowledgment.

Preserve automatic tests, security checks, financial qualification, exact revision verification, maintenance safeguards, and provider concurrency controls. This change does not authorize bypassing a failing automatic check, changing unrelated non-Actions spending caps, reviving retired publishers, or expanding the assigned application work. The separate explicit Actions-budget removal instruction below remains in effect. Required machine checks must actually run and pass. If an active workflow still demands the removed human label, treat that as a source/configuration defect to correct through the existing protected route, not as a reason to request owner approval. Report other demonstrated blockers accurately and continue independent authorized work.

## Current owner handoff, September 17, 2026 UTC

Owner update, September 17: GitHub Actions has no owner-imposed spending budget. Remove Actions-specific account, SKU and repository budgets and stop-usage controls; do not recreate them or require budget approval for authorized delivery. Non-Actions product budgets remain unchanged. The `revert-approved` manual-label dependency is revoked globally. Authorized agents own source review and protected delivery without a human acknowledgment or replacement approval label; required automated checks and production safeguards remain.

This policy supersedes earlier restoration ownership and replay-hold directions. Each task receiving the owner's manual push-and-publish prompt owns integration, publication and live verification of its assigned scope through the restored protected provider route. The owner explicitly directed the restoration task to stop coordinating: agents must not wait for that task, its approval, or completion of earlier numbered deliveries. The previous six-stream release sequence is no longer a release prerequisite. Only actual code/schema dependencies and existing required checks, transactional safeguards, maintenance and provider concurrency controls constrain a particular release. Merely reading this policy does not expand an agent's assigned scope.

The original provider publication routes have been verified with successful production releases: World Hub through GitHub protected merge and Vercel Git source build (`hub-vanguard`); Club Arena client through GitHub and Hetzner; Club Arena engine through the existing GitHub engine release workflows and Hetzner build/cutover. This is publication-route proof, not certification that every pending application package passes its checks. Whole-delivery completion under six minutes remains unproven. The restored application includes recorded owner-authorized corrections and external error telemetry removal; do not describe it as byte-identical historical code or claim the entire application suite is green.

For a manually assigned release: recover the existing owned worktree, branch, PR, source and installed-migration evidence; preserve unrelated work; commit explicit paths with ordinary hooks; push the owned branch; find the existing PR and create one only if absent; run the applicable existing required checks; then complete a protected merge without bypass. Do not depend on disabled agent autopilot or automatic-merge helpers. Use configured authenticated GitHub tools. An access failure in one interface is not proof that all configured access is unavailable.

World Hub: the protected `main` merge triggers the enabled Vercel Git integration for `hub-vanguard`. Verify the production deployment is READY and `https://smarter.poker/api/health` serves the expected merged commit. Do not substitute a local build, prebuilt upload, separate Vercel project, deploy hook or custom publisher.

Club Arena client: the protected `main` merge triggers `publish-club-arena.yml`, which builds through GitHub and publishes to Hetzner. Verify the expected published client revision at both `https://ca-static.smarter.poker/build-info.json` and `https://smarter.poker/hub/club-arena/build-info.json`. Club Arena client publication does not require a World Hub rebuild.

Club Arena engine: use the existing `stage-engine-release.yml` and `auto-deploy-hetzner.yml` route when the assigned change affects the engine. Preserve its required checks, exact revision selection, maintenance window, concurrency and release evidence. The selected runtime component revision can differ from a later documentation-only main revision. Verify the owning release result and the expected component revision in `https://engine.smarter.poker/health`; require the applicable existing post-deployment proof. Database changes require exact installation/readback evidence, and already installed migrations must not be replayed.

Fix only actual blockers using the smallest supported correction. Reuse existing valid evidence; do not rerun successful checks without a source/input change or identified unresolved concern. Outside the explicitly removed approval gate above, never weaken a required check or invent a new release stage. An agent already authorized to push and publish owns intentional-recovery review and proceeds automatically after applicable checks pass, without `revert-approved` or another human/agent approval label. Unintended reversions must still be repaired. Preserve each repository's remaining required automated functional checks; historical-reversion reporting is advisory, and this instruction does not add a new required workflow where none was required. Record pending run/version identities and continue other authorized work while providers run. Do not create a watcher, scheduler or repair loop. Pushed, merged, published and verified live are separate states.

The retired custom/local pipeline, its workers and publishers, release autopilot/watchdog/repair paths, and Sentry integrations remain forbidden. A routine application publish prompt does not authorize their return, a self-hosted upgrade, a new phase of product development, or unrelated cleanup. Use the SSD for preserved source/worktrees as appropriate; production builds currently use the restored providers. Preserve existing protection, financial rules, credentials and production records. Read the hardening standard below and follow later explicit owner instructions.

## Historical records and failed deployment recovery

Older restoration narratives, handoffs and programme plans are records of their dated scope. They do not restore root-only release ownership, numbered delivery waits, global Stage-B freezes, Actions spending controls or manual approval labels. Preserve actual task-specific holds, migration dependencies and transaction safeguards. A historical plan is not a new development assignment.

Owner continuation, September 17: resolve failed deployments immediately; a simple correction must not be parked for an hour by agent policy. Follow the failure classification and recovery rules in the operating law. Diagnose, fix, validate and submit an eligible corrected attempt promptly through its existing owner. Reattach to unknown operations and recover proof without redeploying an already healthy version. Never blind-retry a possible side effect.

The requested durable engine improvement is an additional bounded, certified recovery opportunity when the regular window is missed. This is an implementation requirement, not a claim that the capability is present. Until the maintenance owner, database freeze/thaw, release admission and regression checks implement and verify it together, the existing runtime guards remain binding. Do not reduce proof/rollback reserves, prolong a live freeze or restart active games to avoid a wait. No watcher, timer, recurring agent or second publisher may provide this behavior.

A new explicit owner STOP halts new operations within its scope. This policy does not authorize deleting other agents' work or restarting any retired infrastructure.
